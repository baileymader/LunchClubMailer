<Window x:Class="LunchClubMailer.Views.ImportExport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="SlateGray" MaxHeight="300" MinHeight="300" MaxWidth="300" MinWidth="300">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="1*" />
      <RowDefinition Height="1*" />
      <RowDefinition Height="1*" />
    </Grid.RowDefinitions>

    
    <Button Content="Get Import Template File" Command="{Binding TemplateFileCommand}" Grid.Row="0" Margin="5"/>

    <Button Content="Import File" Command="{Binding ImportFileCommand}" Grid.Row="1" Margin="5" />

    <Button Content="Export File" Command="{Binding ExportFileCommand}" Grid.Row="2" Margin="5" />

  </Grid>
</Window>
